local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
local Yellow = Color3.fromHex("#FFD21F")
local DarkYellow = Color3.fromHex("#9C7800")
local Black = Color3.fromHex("#080808")
local Window = WindUI:CreateWindow({
    Title = "Chituk Hub",
    Icon = "rbxthumb://type=Asset&id=109365656627783&w=150&h=150",
    Folder = "ChitukHub",
    NewElements = true,
    Size = UDim2.fromOffset(520, 440),
    OpenButton = {
        Title = "Chituk Hub",
        CornerRadius = UDim.new(1, 0),
        StrokeThickness = 2,
        Enabled = true,
        Draggable = true,
        OnlyMobile = false,
        Scale = 1.0,
        Color = ColorSequence.new(Yellow, DarkYellow)
    },
    Topbar = {
        Height = 44,
        ButtonsType = "Default"
    }
})
local MainSection = Window:Section({Title = "Main"})
local ProteccionesSection = Window:Section({Title = "Protecciones"})
local WeaponsSection = Window:Section({Title = "Armas"})
local ESPSection = Window:Section({Title = "ESP"})
local TPSection = Window:Section({Title = "TP"})
local AimSection = Window:Section({Title = "AimBot"})
local DiversionSection = Window:Section({Title = "Diversion"})
local CreditsSection = Window:Section({Title = "Creditos"})
local MainTab = MainSection:Tab({
    Title = "Main",
    Icon = "home",
    IconColor = Yellow,
    IconShape = "Square",
    Border = true
})
MainTab:Section({Title = "CHITUK HUB", TextSize = 24, FontWeight = Enum.FontWeight.SemiBold})
MainTab:Section({Title = "Bienvenido a Chituk Hub", TextSize = 16, TextTransparency = 0.15})
MainTab:Section({
    Title = "⚠️ ADVERTENCIA IMPORTANTE",
    TextSize = 18,
    TextColor = Color3.fromRGB(255, 50, 50)
})
MainTab:Section({
    Title = "¿Estás seguro que quieres usar este script?\nPodrías ser baneado de Prison Life al usarlo incorrectamente.",
    TextSize = 14,
    TextColor = Color3.fromRGB(255, 200, 200),
    TextTransparency = 0.1
})
local vflyGui = nil
MainTab:Toggle({
    Title = "Vfly (Funciona Con Autos y tambien es un Fly Normal)",
    Default = false,
    Callback = function(value)
        if value then
            local LocalPlayer = game.Players.LocalPlayer
            if not LocalPlayer.Character then
                LocalPlayer.CharacterAdded:Wait()
            end
            local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 10)
            local CurrentCamera = workspace.CurrentCamera
            local RunService = game:GetService("RunService")
            local u5 = false
            local FindFirstChild = PlayerGui.FindFirstChild
            local n1 = 1
            local u8 = false
            if FindFirstChild(PlayerGui, "ChitukHubFlyGui") then
                PlayerGui.ChitukHubFlyGui:Destroy()
            end
            local ScreenGui = Instance.new("ScreenGui", PlayerGui)
            vflyGui = ScreenGui
            ScreenGui.Name = "ChitukHubFlyGui"
            ScreenGui.ResetOnSpawn = false
            local Frame = Instance.new("Frame", ScreenGui)
            Frame.Size = UDim2.new(0, 220, 0, 170)
            Frame.Position = UDim2.new(0.5, -110, 0.25, 0)
            Frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
            Frame.BackgroundTransparency = 0.2
            Frame.Active = true
            Frame.Draggable = true
            Frame.ClipsDescendants = true
            Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 10)
            local TextLabel = Instance.new("TextLabel", Frame)
            TextLabel.Size = UDim2.new(1, 0, 0, 40)
            TextLabel.Position = UDim2.new(0, 0, 0, 0)
            TextLabel.Text = "Chituk Hub Fly Gui"
            TextLabel.TextColor3 = Color3.new(1, 1, 1)
            TextLabel.BackgroundTransparency = 1
            TextLabel.Font = Enum.Font.SourceSansBold
            TextLabel.TextSize = 20
            TextLabel.TextXAlignment = Enum.TextXAlignment.Center
            TextLabel.TextYAlignment = Enum.TextYAlignment.Center
            local TextButton = Instance.new("TextButton", Frame)
            TextButton.Size = UDim2.new(0, 30, 0, 30)
            TextButton.Position = UDim2.new(1, -35, 0, 5)
            TextButton.Text = "V"
            TextButton.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
            TextButton.BackgroundTransparency = 0.4
            TextButton.TextColor3 = Color3.new(1, 1, 1)
            TextButton.Font = Enum.Font.SourceSansBold
            TextButton.TextSize = 18
            TextButton.ZIndex = 2
            Instance.new("UICorner", TextButton).CornerRadius = UDim.new(0, 5)
            local TextBox = Instance.new("TextBox", Frame)
            TextBox.Size = UDim2.new(0, 70, 0, 60)
            TextBox.Position = UDim2.new(0, 75, 0, 45)
            TextBox.Text = "1"
            TextBox.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
            TextBox.BackgroundTransparency = 0
            TextBox.TextColor3 = Color3.new(1, 1, 1)
            TextBox.Font = Enum.Font.SourceSansBold
            TextBox.TextSize = 30
            TextBox.ClearTextOnFocus = false
            Instance.new("UICorner", TextBox)
            local TextButton2 = Instance.new("TextButton", Frame)
            TextButton2.Size = UDim2.new(0, 55, 0, 60)
            TextButton2.Position = UDim2.new(0, 15, 0, 45)
            TextButton2.Text = "-"
            TextButton2.BackgroundColor3 = Color3.fromRGB(90, 90, 90)
            TextButton2.BackgroundTransparency = 0
            TextButton2.TextColor3 = Color3.new(1, 1, 1)
            TextButton2.TextSize = 40
            Instance.new("UICorner", TextButton2)
            local TextButton3 = Instance.new("TextButton", Frame)
            TextButton3.Size = UDim2.new(0, 55, 0, 60)
            TextButton3.Position = UDim2.new(0, 150, 0, 45)
            TextButton3.Text = "+"
            TextButton3.BackgroundColor3 = Color3.fromRGB(90, 90, 90)
            TextButton3.BackgroundTransparency = 0
            TextButton3.TextColor3 = Color3.new(1, 1, 1)
            TextButton3.TextSize = 40
            Instance.new("UICorner", TextButton3)
            local TextButton4 = Instance.new("TextButton", Frame)
            TextButton4.Size = UDim2.new(1, -30, 0, 45)
            TextButton4.Position = UDim2.new(0, 15, 0, 115)
            TextButton4.Text = "Fly"
            TextButton4.BackgroundColor3 = Color3.fromRGB(110, 110, 110)
            TextButton4.BackgroundTransparency = 0
            TextButton4.TextColor3 = Color3.new(1, 1, 1)
            TextButton4.Font = Enum.Font.SourceSans
            TextButton4.TextSize = 35
            Instance.new("UICorner", TextButton4)
            TextButton.MouseButton1Click:Connect(function()
                u8 = not u8
                if u8 then
                    Frame:TweenSize(UDim2.new(0, 220, 0, 40), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.3, true)
                    TextButton.Text = ">"
                    return
                end
                Frame:TweenSize(UDim2.new(0, 220, 0, 170), Enum.EasingDirection.Out, Enum.EasingStyle.Quart, 0.3, true)
                TextButton.Text = "V"
            end)
            TextButton3.MouseButton1Click:Connect(function()
                n1 += 1
                TextBox.Text = tostring(n1)
            end)
            TextButton2.MouseButton1Click:Connect(function()
                n1 -= 1
                TextBox.Text = tostring(n1)
            end)
            TextBox.FocusLost:Connect(function(_)
                local num = tonumber(TextBox.Text)
                if num then
                    n1 = num
                    return
                end
                TextBox.Text = tostring(n1)
            end)
            local function v17(p2)
                local vector3 = Vector3.new(0, 0, 0)
                if p2 and p2:IsA("VehicleSeat") then
                    local ThrottleFloat = p2.ThrottleFloat
                    local SteerFloat = p2.SteerFloat
                    if ThrottleFloat ~= 0 or SteerFloat ~= 0 then
                        vector3 = CurrentCamera.CFrame.LookVector * ThrottleFloat + CurrentCamera.CFrame.RightVector * SteerFloat
                    end
                end
                if vector3.Magnitude == 0 then
                    local Character = LocalPlayer.Character
                    local v29 = Character and Character:FindFirstChildOfClass("Humanoid")
                    local v30 = v29
                    if v29 then
                        v30 = v29.MoveDirection.Magnitude > 0
                    end
                    if v30 then
                        local MoveDirection = v29.MoveDirection
                        local v32 = MoveDirection:Dot(CurrentCamera.CFrame.LookVector)
                        local v33 = MoveDirection:Dot(CurrentCamera.CFrame.RightVector)
                        vector3 = CurrentCamera.CFrame.LookVector * v32 + CurrentCamera.CFrame.RightVector * v33
                    end
                end
                local v34 = vector3.Magnitude > 0 and vector3.Unit
                if not v34 then
                    v34 = Vector3.new(0, 0, 0)
                end
                return v34
            end)
            TextButton4.MouseButton1Click:Connect(function()
                u5 = not u5
                TextButton4.Text = not u5 and "Fly" or "UnFly"
                if u5 then
                    local Character = LocalPlayer.Character
                    local Humanoid = Character:FindFirstChildOfClass("Humanoid")
                    local v37 = Humanoid and Humanoid.SeatPart
                    local v38 = v37
                    if not v38 then
                        if Character then
                            Character = Character:FindFirstChild("HumanoidRootPart")
                        end
                        v38 = Character
                    end
                    local u39 = v38
                    local v40 = u39
                    if v40 then
                        v40 = u39:IsA("VehicleSeat")
                    end
                    if v40 then
                        u39 = u39.Parent.PrimaryPart or u39
                    end
                    if u39 then
                        local BodyVelocity = Instance.new("BodyVelocity", u39)
                        BodyVelocity.maxForce = Vector3.new(9000000000, 9000000000, 9000000000)
                        local BodyGyro = Instance.new("BodyGyro", u39)
                        BodyGyro.maxTorque = Vector3.new(9000000000, 9000000000, 9000000000)
                        BodyGyro.P = 5000
                        task.spawn(function()
                            while true do
                                local v48 = u5
                                if v48 then
                                    v48 = u39
                                    if v48 then
                                        v48 = u39.Parent
                                    end
                                end
                                if not v48 then
                                    break
                                end
                                local v49 = v17(v37)
                                if v49.Magnitude > 0 then
                                    BodyVelocity.velocity = v49 * (n1 * 50)
                                else
                                    BodyVelocity.velocity = Vector3.new(0, 0.1, 0)
                                end
                                BodyGyro.cframe = CurrentCamera.CFrame
                                RunService.Heartbeat:Wait()
                            end
                            if BodyVelocity then
                                BodyVelocity:Destroy()
                            end
                            if BodyGyro then
                                BodyGyro:Destroy()
                            end
                        end)
                    end
                end
            end)
        else
            if vflyGui then
                vflyGui:Destroy()
                vflyGui = nil
            end
        end
    end
})
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local root = character:WaitForChild("HumanoidRootPart")
local speedMultiplier = 4
local speedEnabled = false
local BASE_SPEED = 16
local speedConnection
local function activarVelocidad()
    if speedConnection then speedConnection:Disconnect() end
    speedConnection = RunService.RenderStepped:Connect(function(dt)
        if not speedEnabled then return end
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if char and hrp and hrp.Parent then
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.MoveDirection.Magnitude > 0 then
                local CF_SPEED = BASE_SPEED * speedMultiplier
                hrp.CFrame = hrp.CFrame + (humanoid.MoveDirection * CF_SPEED * dt)
            end
        end
    end)
end
player.CharacterAdded:Connect(function(char)
    character = char
    root = char:WaitForChild("HumanoidRootPart")
    if speedEnabled then
        activarVelocidad()
    end
end)
MainTab:Toggle({
    Title = "Activar Velocidad",
    Default = false,
    Callback = function(value)
        speedEnabled = value
        if value then
            activarVelocidad()
        elseif speedConnection then
            speedConnection:Disconnect()
            speedConnection = nil
        end
    end
})
MainTab:Input({
    Title = "Velocidad (limite 300)",
    Default = "4",
    Numeric = true,
    Callback = function(value)
        local num = tonumber(value)
        if num then
            speedMultiplier = math.clamp(num, 0, 300)
        end
    end
})
local UIS = game:GetService("UserInputService")
local infiniteJumpEnabled = false
local jumpConnection
local function activarSaltoInfinito()
    if jumpConnection then jumpConnection:Disconnect() end
    jumpConnection = UIS.JumpRequest:Connect(function()
        if not infiniteJumpEnabled then return end
        local character = player.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end
MainTab:Toggle({
    Title = "Salto Infinito",
    Default = false,
    Callback = function(value)
        infiniteJumpEnabled = value
        if value then
            activarSaltoInfinito()
        elseif jumpConnection then
            jumpConnection:Disconnect()
            jumpConnection = nil
        end
    end
})
local crosshairGui = nil
MainTab:Button({
    Title = "CrossHair (Mira)",
    Callback = function()
        if not crosshairGui then
            crosshairGui = Instance.new("ScreenGui")
            crosshairGui.Name = "CrossHairChituk"
            crosshairGui.ResetOnSpawn = false
            local mira = Instance.new("ImageLabel")
            mira.AnchorPoint = Vector2.new(0.5, 0.5)
            mira.Position = UDim2.new(0.5, 0, 0.5, 0)
            mira.BackgroundTransparency = 1
            mira.Image = "rbxassetid://7905983439"
            mira.Size = UDim2.new(0, 50, 0, 50)
            mira.Parent = crosshairGui
            crosshairGui.Parent = game.Players.LocalPlayer.PlayerGui
        else
            crosshairGui.Enabled = not crosshairGui.Enabled
        end
    end
})
local Lighting = game:GetService("Lighting")
local fullBrightEnabled = false
local oldLighting = {
    Brightness = Lighting.Brightness,
    Ambient = Lighting.Ambient,
    OutdoorAmbient = Lighting.OutdoorAmbient,
    ColorShift_Top = Lighting.ColorShift_Top,
    ColorShift_Bottom = Lighting.ColorShift_Bottom,
    ShadowSoftness = Lighting.ShadowSoftness,
    GlobalShadows = Lighting.GlobalShadows,
    FogEnd = Lighting.FogEnd,
    FogStart = Lighting.FogStart
}
MainTab:Toggle({
    Title = "FullBright / Iluminacion Completa",
    Default = false,
    Callback = function(value)
        fullBrightEnabled = value
        if value then
            Lighting.Brightness = 3
            Lighting.Ambient = Color3.fromRGB(255, 255, 255)
            Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
            Lighting.ColorShift_Top = Color3.fromRGB(255, 255, 255)
            Lighting.ColorShift_Bottom = Color3.fromRGB(255, 255, 255)
            Lighting.ShadowSoftness = 1
            Lighting.GlobalShadows = false
            Lighting.FogEnd = 100000
            Lighting.FogStart = 100000
        else
            Lighting.Brightness = oldLighting.Brightness
            Lighting.Ambient = oldLighting.Ambient
            Lighting.OutdoorAmbient = oldLighting.OutdoorAmbient
            Lighting.ColorShift_Top = oldLighting.ColorShift_Top
            Lighting.ColorShift_Bottom = oldLighting.ColorShift_Bottom
            Lighting.ShadowSoftness = oldLighting.ShadowSoftness
            Lighting.GlobalShadows = oldLighting.GlobalShadows
            Lighting.FogEnd = oldLighting.FogEnd
            Lighting.FogStart = oldLighting.FogStart
        end
    end
})
local fpsBoostEnabled = false
local oldEffects = {}
local oldMaterials = {}
local oldShadows = {}
local oldMaterialQuality = UserSettings():GetService("UserGameSettings").SavedQualityLevel
local function desactivarEfectos()
    for _, effect in ipairs(Lighting:GetChildren()) do
        if effect:IsA("PostEffect") or effect:IsA("BlurEffect") or effect:IsA("BloomEffect")
            or effect:IsA("DepthOfFieldEffect") or effect:IsA("SunRaysEffect")
            or effect:IsA("ColorCorrectionEffect") then
            oldEffects[effect] = effect.Enabled
            effect.Enabled = false
        end
    end
    for _, desc in ipairs(workspace:GetDescendants()) do
        if desc:IsA("BasePart") then
            oldMaterials[desc] = desc.Material
            oldShadows[desc] = desc.CastShadow
            desc.Material = Enum.Material.SmoothPlastic
            desc.CastShadow = false
        end
    end
    UserSettings():GetService("UserGameSettings").SavedQualityLevel = Enum.SavedQualitySetting.QualityLevel1
    Lighting.GlobalShadows = false
    Lighting.ShadowSoftness = 1
end
local function restaurarEfectos()
    for effect, enabled in pairs(oldEffects) do
        if effect and effect.Parent then
            effect.Enabled = enabled
        end
    end
    oldEffects = {}
    for part, material in pairs(oldMaterials) do
        if part and part.Parent then
            part.Material = material
        end
    end
    oldMaterials = {}
    for part, castShadow in pairs(oldShadows) do
        if part and part.Parent then
            part.CastShadow = castShadow
        end
    end
    oldShadows = {}
    UserSettings():GetService("UserGameSettings").SavedQualityLevel = oldMaterialQuality
    Lighting.GlobalShadows = oldLighting.GlobalShadows
    Lighting.ShadowSoftness = oldLighting.ShadowSoftness
end
MainTab:Toggle({
    Title = "FPS Boost",
    Default = false,
    Callback = function(value)
        fpsBoostEnabled = value
        if value then
            desactivarEfectos()
        else
            restaurarEfectos()
        end
    end
})
MainTab:Button({
    Title = "Auto Escapar Prision",
    Description = "Teletransporta al contenedor y se suicida",
    Callback = function()
        local players = game:GetService("Players")
        local lp = players.LocalPlayer
        local chr = lp.Character or lp.CharacterAdded:Wait()
        local hrp = chr:WaitForChild("HumanoidRootPart")
        local tpto = workspace.Shippingcontainers:GetChildren()[6]:GetChildren()[2]
        hrp.CFrame = tpto.CFrame
        wait(2)
        local hum = chr:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.Health = 0
        end
    end
})
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local remote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("GiverPressed")
local RETRY_DELAY = 0.3
local function getToolFromRS()
    local toolsFolder = ReplicatedStorage:FindFirstChild("Tools")
    if not toolsFolder then return end
    return toolsFolder:FindFirstChild("Key card", true)
end
local function getToolFromPlayers()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            local backpack = p:FindFirstChild("Backpack")
            local tool = backpack and backpack:FindFirstChild("Key card")
            if tool then
                return tool
            end
        end
    end
end
local function tryGiveKeycard()
    local backpack = player:WaitForChild("Backpack")
    if backpack:FindFirstChild("Key card") then
        return true
    end
    if player.Team and player.Team.Name == "Guards" then
        return true
    end
    local source = getToolFromRS() or getToolFromPlayers()
    if source then
        source:Clone().Parent = backpack
        return true
    end
    return false
end
MainTab:Button({
    Title = "Auto Conseguir Keycard",
    Description = "Obtiene la keycard automaticamente",
    Callback = function()
        task.spawn(function()
            local running = true
            local obtained = false
            local startTime = tick()
            local timeout = 10
            while running and tick() - startTime < timeout do
                local success = pcall(tryGiveKeycard)
                if success and tryGiveKeycard() then
                    obtained = true
                    running = false
                    break
                end
                task.wait(RETRY_DELAY)
            end
            if obtained then
                WindUI:Notify({Title = "Exito", Description = "Keycard obtenida correctamente", Duration = 2})
            else
                WindUI:Notify({Title = "Error", Description = "No se pudo obtener la keycard", Duration = 2})
            end
        end)
    end
})
local gunPositions = {
    ["Remington 870"] = CFrame.new(820.298583984375, 87.85000610351562, 2229.396240234375),
    ["MP5"] = CFrame.new(813.6986694335938, 87.85000610351562, 2229.396240234375),
    ["AK-47"] = CFrame.new(-931.7928466796875, 81.27831268310547, 2039.2554931640625),
}
local touchGiverCache = {}
local function getTouchGiver(gunName)
    if touchGiverCache[gunName] then
        return touchGiverCache[gunName]
    end
    local targetPos = gunPositions[gunName]
    if not targetPos then return nil end
    for _, weapongiver in ipairs(workspace:QueryDescendants("#TouchGiver")) do
        if weapongiver and weapongiver:IsA("Model") then
            if weapongiver:GetPivot().Position == targetPos.Position + Vector3.new(0, 10, 0) then
                touchGiverCache[gunName] = weapongiver
                return weapongiver
            end
        end
    end
    return nil
end
local function hasGun(name)
    local backpack = player:FindFirstChild("Backpack")
    local char = player.Character or player.CharacterAdded:Wait()
    return (backpack and backpack:FindFirstChild(name)) or (char and char:FindFirstChild(name))
end
local function GetGun(gunName)
    local targetPos = gunPositions[gunName]
    if not targetPos then
        WindUI:Notify({Title = "Error", Description = "Posición no encontrada para " .. gunName, Duration = 3})
        return false
    end
    local targetGiver = getTouchGiver(gunName)
    if not targetGiver then
        WindUI:Notify({Title = "Error", Description = "No se encontró el TouchGiver para " .. gunName, Duration = 2})
        return false
    end
    local char = player.Character
    if not char then
        WindUI:Notify({Title = "Error", Description = "Personaje no encontrado", Duration = 2})
        return false
    end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    local oldPos = hrp.CFrame
    local success = false
    local startTime = tick()
    local timeout = 2
    local conn
    conn = RunService.Heartbeat:Connect(function()
        if not char or not char.Parent then
            conn:Disconnect()
            return
        end
        hrp.CFrame = targetPos
        pcall(function()
            remote:FireServer(targetGiver)
        end)
        if hasGun(gunName) then
            success = true
            conn:Disconnect()
            hrp.CFrame = oldPos
        end
    end)
    while not success and tick() - startTime < timeout do
        task.wait(0.1)
    end
    if conn and conn.Connected then
        conn:Disconnect()
        hrp.CFrame = oldPos
    end
    if success then
        WindUI:Notify({Title = "Exito", Description = "Obtuviste: " .. gunName, Duration = 2})
    else
        WindUI:Notify({Title = "Error", Description = "No se pudo obtener " .. gunName, Duration = 2})
    end
    return success
end
local function GrabAllGuns()
    local obtained = {}
    for _, gun in ipairs({"Remington 870", "MP5", "AK-47"}) do
        if not hasGun(gun) then
            if GetGun(gun) then
                table.insert(obtained, gun)
            end
            task.wait(0.2)
        end
    end
    if #obtained == 0 then
        WindUI:Notify({Title = "Info", Description = "Ya tienes todas las armas", Duration = 2})
    else
        WindUI:Notify({Title = "Listo", Description = "Obtuviste " .. #obtained .. " arma(s)", Duration = 2})
    end
end
local ProteccionesTab = ProteccionesSection:Tab({
    Title = "Protecciones",
    Icon = "shield",
    IconColor = Yellow,
    IconShape = "Square",
    Border = true
})
ProteccionesTab:Section({Title = "PROTECCIONES", TextSize = 24, FontWeight = Enum.FontWeight.SemiBold})
ProteccionesTab:Section({Title = "Anti Arrest y Anti Taser", TextSize = 16, TextTransparency = 0.15})
local antiArrestEnabled = false
local antiTaseEnabled = false
local animConnection = nil
local ARREST_ANIMATION = "rbxassetid://287112271"
local TASE_ANIMATION = "rbxassetid://279227693"
local function setupProtectionConnection()
    if animConnection then animConnection:Disconnect() end
    local function onCharacterAdded(char)
        local humanoid = char:WaitForChild("Humanoid")
        local animator = humanoid:WaitForChild("Animator")
        animator.AnimationPlayed:Connect(function(des)
            if antiArrestEnabled and des.Animation.AnimationId == ARREST_ANIMATION then
                local Humanoid = player.Character:WaitForChild("Humanoid")
                Humanoid:ChangeState(Enum.HumanoidStateType.Dead)
                local cpos = char:WaitForChild("HumanoidRootPart").CFrame
                local wasCriminal = (player.TeamColor.Name == "Really red")
                player.CharacterAdded:Wait()
                repeat task.wait() until player.Character:FindFirstChild("HumanoidRootPart")
                task.wait(0.1)
                if wasCriminal then
                    pcall(function()
                        player.Character.HumanoidRootPart.CFrame = cpos
                    end)
                end
            end
            if antiTaseEnabled and des.Animation.AnimationId == TASE_ANIMATION then
                des:Stop()
                des:Destroy()
                player.Character.Humanoid.PlatformStand = false
                player.Character.Humanoid.Sit = false
                player.Character.Humanoid.WalkSpeed = 16
                player.Character.Humanoid.JumpPower = 50
            end
        end)
    end
    if player.Character then
        onCharacterAdded(player.Character)
    end
    animConnection = player.CharacterAdded:Connect(onCharacterAdded)
end
ProteccionesTab:Toggle({
    Title = "Anti Arrest",
    Default = false,
    Callback = function(value)
        antiArrestEnabled = value
        if (antiArrestEnabled or antiTaseEnabled) and not animConnection then
            setupProtectionConnection()
        end
    end
})
ProteccionesTab:Toggle({
    Title = "Anti Taser",
    Default = false,
    Callback = function(value)
        antiTaseEnabled = value
        if (antiArrestEnabled or antiTaseEnabled) and not animConnection then
            setupProtectionConnection()
        end
    end
})
local WeaponsTab = WeaponsSection:Tab({
    Title = "Armas",
    Icon = "gun",
    IconColor = Yellow,
    IconShape = "Square",
    Border = true
})
WeaponsTab:Section({Title = "Consigue Las Armas Disponibles De Una Manera mas facil", TextSize = 20, FontWeight = Enum.FontWeight.SemiBold})
WeaponsTab:Button({
    Title = "Obtener Todas las Armas",
    Description = "TP automatico a todos los spawners",
    Callback = function()
        task.spawn(GrabAllGuns)
    end
})
WeaponsTab:Section({Title = "Seleccionar Arma Especifica", TextSize = 14, TextTransparency = 0.15})
local allGuns = {"Remington 870", "MP5", "AK-47"}
local selectedGun = allGuns[1]
WeaponsTab:Dropdown({
    Title = "Arma",
    Values = allGuns,
    Default = allGuns[1],
    Callback = function(value)
        selectedGun = value
    end
})
WeaponsTab:Button({
    Title = "Obtener Arma Seleccionada",
    Description = "TP al spawner del arma elegida",
    Callback = function()
        if hasGun(selectedGun) then
            WindUI:Notify({Title = "Info", Description = "Ya tienes: " .. selectedGun, Duration = 2})
        else
            task.spawn(function() GetGun(selectedGun) end)
        end
    end
})
local DiversionTab = DiversionSection:Tab({
    Title = "Diversion",
    Icon = "gamepad",
    IconColor = Yellow,
    IconShape = "Square",
    Border = true
})
DiversionTab:Section({Title = "DIVERSION", TextSize = 24, FontWeight = Enum.FontWeight.SemiBold})
DiversionTab:Section({Title = "Funciones divertidas", TextSize = 16, TextTransparency = 0.15})
local airWalkEnabled = false
local airPart = nil
local airWalkConnection
local function createAirPart()
    if airPart then
        airPart:Destroy()
    end
    airPart = Instance.new("Part")
    airPart.Name = "Airwalk"
    airPart.Size = Vector3.new(7, 2, 3)
    airPart.Transparency = 1
    airPart.Anchored = true
    airPart.CanCollide = false
    airPart.Parent = workspace
end
local function startAirWalk()
    createAirPart()
    airWalkConnection = RunService.RenderStepped:Connect(function()
        if not airWalkEnabled then return end
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hrp and airPart then
            airPart.CFrame = hrp.CFrame + Vector3.new(0, -4, 0)
            airPart.CanCollide = true
        end
    end)
end
local function stopAirWalk()
    if airWalkConnection then
        airWalkConnection:Disconnect()
        airWalkConnection = nil
    end
    if airPart then
        airPart:Destroy()
        airPart = nil
    end
end
DiversionTab:Toggle({
    Title = "Air Walk",
    Default = false,
    Callback = function(value)
        airWalkEnabled = value
        if value then
            startAirWalk()
        else
            stopAirWalk()
        end
    end
})
local FlipCooldown = false
local frontFlipGui = nil
local function FortniteFlips()
    if FlipCooldown then
        return
    end
    FlipCooldown = true
    local character = player.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    local animator = humanoid and humanoid:FindFirstChildOfClass("Animator")
    local animate = character and character:FindFirstChild("Animate")
    if not hrp or not humanoid then
        FlipCooldown = false
        return
    end
    local savedTracks = {}
    if animator then
        for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
            savedTracks[#savedTracks + 1] = {
                track = track,
                time = track.TimePosition,
                speed = track.Speed,
                weight = track.WeightCurrent
            }
            track:Stop(0)
        end
    end
    local animateWasEnabled = animate and animate.Enabled
    if animate then
        animate.Enabled = false
    end
    local oldAutoRotate = humanoid.AutoRotate
    humanoid.AutoRotate = false
    humanoid:ChangeState(Enum.HumanoidStateType.Physics)
    local duration = 0.45
    local steps = 120
    local startCFrame = hrp.CFrame
    local forwardVector = startCFrame.LookVector
    local upVector = Vector3.new(0, 1, 0)
    task.spawn(function()
        local startTime = tick()
        for i = 1, steps do
            if not character.Parent or humanoid.Health <= 0 then
                break
            end
            local t = i / steps
            local height = 4 * (t - t ^ 2) * 10
            local nextPos =
                startCFrame.Position
                + forwardVector * (35 * t)
                + upVector * height
            local rotation =
                startCFrame.Rotation
                * CFrame.Angles(
                    -math.rad(i * (360 / steps)),
                    0,
                    0
                )
            hrp.CFrame = CFrame.new(nextPos) * rotation
            local elapsedTime = tick() - startTime
            local expectedTime = (duration / steps) * i
            local waitTime = expectedTime - elapsedTime
            if waitTime > 0 then
                task.wait(waitTime)
            end
        end
        if character.Parent and humanoid.Health > 0 then
            hrp.CFrame =
                CFrame.new(startCFrame.Position + forwardVector * 35)
                * startCFrame.Rotation
            humanoid.AutoRotate = oldAutoRotate
            if animate then
                animate.Enabled = animateWasEnabled ~= false
            end
            humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            task.wait(0.05)
            humanoid:ChangeState(Enum.HumanoidStateType.Running)
            task.wait(0.1)
            if animator then
                for _, data in ipairs(savedTracks) do
                    if data.track and data.track.Parent then
                        pcall(function()
                            data.track:Play(
                                0.1,
                                data.weight,
                                data.speed
                            )
                            data.track.TimePosition = data.time
                        end)
                    end
                end
            end
        end
        task.wait(0.25)
        FlipCooldown = false
    end)
end
local function createFrontFlipGui()
    if frontFlipGui then
        frontFlipGui:Destroy()
    end
    local UserInputService = game:GetService("UserInputService")
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "ChitukHubFrontFlip"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.Parent = game:GetService("CoreGui")
    frontFlipGui = ScreenGui
    local Main = Instance.new("Frame")
    Main.Size = UDim2.new(0, 160, 0, 85)
    Main.Position = UDim2.new(0.5, -80, 0.75, 0)
    Main.BackgroundTransparency = 1
    Main.Active = true
    Main.Parent = ScreenGui
    local Title = Instance.new("TextLabel")
    Title.Size = UDim2.new(1, 0, 0, 25)
    Title.Position = UDim2.new(0, 0, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "Chituk Hub-FrontFlip"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextTransparency = 0.45
    Title.TextSize = 14
    Title.Font = Enum.Font.GothamMedium
    Title.Parent = Main
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0, 140, 0, 50)
    Button.Position = UDim2.new(0.5, -70, 0, 30)
    Button.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Button.BorderSizePixel = 0
    Button.Text = "FRONT FLIP"
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 17
    Button.Font = Enum.Font.GothamBold
    Button.AutoButtonColor = true
    Button.Parent = Main
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Button
    local dragging = false
    local dragStart
    local startPos
    Main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = Main.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if not dragging then
            return
        end
        if input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch then
            local delta = input.Position - dragStart
            Main.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
    Button.Activated:Connect(function()
        FortniteFlips()
    end)
end
local frontFlipToggle
frontFlipToggle = DiversionTab:Toggle({
    Title = "FrontFlip",
    Default = false,
    Callback = function(value)
        if value then
            createFrontFlipGui()
        else
            if frontFlipGui then
                frontFlipGui:Destroy()
                frontFlipGui = nil
            end
        end
    end
})
local botActivo = false
local botConexiones = {}
local botHilos = {}
local function toggleBot(value)
    if value then
        if botActivo then return end
        botActivo = true
        local sujetos = {
            "Yo", "Tú", "Él", "Ella", "Nosotros", "Ellos", "Mis amigos",
            "Mi equipo", "El juego", "El mapa", "Los jugadores",
            "La comunidad", "Mi grupo", "Los novatos", "Los veteranos"
        }
        local verbos = {
            "quiero", "disfruto", "me encanta", "prefiero", "necesito",
            "juego", "exploro", "salto", "corro", "gano", "pierdo", "construyo",
            "peleo", "colecciono", "camino", "vuelo", "bailo", "canto", "ayudo",
            "compito", "colaboro", "sobrevivo", "me divierto", "aventuro"
        }
        local objetos = {
            "Roblox", "los juegos", "la diversión", "la aventura", "las misiones",
            "el mapa", "el equipo", "la base", "las monedas", "el botín",
            "las mascotas", "los amigos", "el lobby", "el mundo", "los jugadores",
            "la victoria", "los vehículos", "las herramientas", "las skins",
            "los sombreros", "las espadas", "la batalla", "el tesoro"
        }
        local complementos = {
            "con mis amigos", "en equipo", "juntos", "rápidamente", "ahora mismo",
            "más tarde", "en el lobby", "en el juego", "muy bien", "genial",
            "increíble", "por aquí", "por allá", "solos", "tranquilos", "emocionados",
            "sin parar", "a toda velocidad", "con alegría", "sin miedo"
        }
        local frasesHechas = {
            "¡Hola a todos!", "¿Cómo están?", "¡Vamos a jugar!", "Qué divertido es esto",
            "Esto es increíble", "Exploremos el mapa", "Gané la partida", "Perdí esta vez",
            "Buena partida amigos", "¡Equipo, unidos!", "Amo Roblox",
            "Salten conmigo", "Corran rápido", "¡Vamos por la victoria!",
            "Construyamos algo épico", "El mapa es enorme", "Los amigos son lo mejor",
            "A divertirse", "¿Alguien quiere jugar?", "Esto se pone emocionante",
            "Me encanta este juego", "Vamos todos juntos", "Esto es épico"
        }
        local frasesMuerte = {
            "¡Eso Dolió!",
            "¡No La próxima!",
            "¡Triste...!",
            "¡Intentaré de Nuevo!",
            "¡La Muerte No Acabará Con Mi Aventura Infinita!"
        }
        local function generarFrase()
            local s = sujetos[math.random(#sujetos)]
            local v = verbos[math.random(#verbos)]
            local o = objetos[math.random(#objetos)]
            if math.random() < 0.6 then
                local c = complementos[math.random(#complementos)]
                return s .. " " .. v .. " " .. o .. " " .. c
            else
                return s .. " " .. v .. " " .. o
            end
        end
        local function obtenerFrase()
            if math.random() < 0.25 then
                return frasesHechas[math.random(#frasesHechas)]
            else
                return generarFrase()
            end
        end
        local replicatedStorage = game:GetService("ReplicatedStorage")
        local chatEvent = replicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
        if chatEvent then chatEvent = chatEvent:FindFirstChild("SayMessageRequest") end
        local chatService = game:GetService("Chat")
        local function hablar(mensaje)
            if chatEvent then
                chatEvent:FireServer(mensaje, "All")
            else
                local character = player.Character
                if character then
                    local head = character:FindFirstChild("Head")
                    if head then
                        chatService:Chat(head, mensaje)
                    end
                end
            end
        end
        local function obtenerPosicionAleatoria(raiz)
            local centro = raiz and raiz.Position or Vector3.new(0, 0, 0)
            local radio = 45
            local angulo = math.random() * 2 * math.pi
            local distancia = 15 + math.random() * (radio - 15)
            local x = centro.X + math.cos(angulo) * distancia
            local z = centro.Z + math.sin(angulo) * distancia
            local y = raiz and raiz.Position.Y or 0
            return Vector3.new(x, y, z)
        end
        local yaDichoMuerte = false
        local destinoActual = nil
        local tiempoSalto = 0
        local INTERVALO_SALTO = 4.5
        local function obtenerPersonaje()
            local char = player.Character or player.CharacterAdded:Wait()
            local hum = char:FindFirstChildOfClass("Humanoid")
            return char, hum
        end
        local char, hum = obtenerPersonaje()
        local function onDied()
            if not botActivo then return end
            if not yaDichoMuerte then
                yaDichoMuerte = true
                local frase = frasesMuerte[math.random(#frasesMuerte)]
                hablar(frase)
            end
        end
        if hum then
            hum.Died:Connect(onDied)
        end
        local charAddedConn = player.CharacterAdded:Connect(function(newChar)
            char = newChar
            hum = char:WaitForChild("Humanoid")
            yaDichoMuerte = false
            destinoActual = nil
            hum.Died:Connect(onDied)
        end)
        table.insert(botConexiones, charAddedConn)
        local function elegirNuevoDestino()
            local raiz = char:FindFirstChild("HumanoidRootPart")
            if raiz then
                destinoActual = obtenerPosicionAleatoria(raiz)
                hum:MoveTo(destinoActual)
            end
        end
        local hiloMov = task.spawn(function()
            while botActivo do
                task.wait(0.3)
                if hum and hum.Health > 0 then
                    local raiz = char:FindFirstChild("HumanoidRootPart")
                    if raiz then
                        if not destinoActual then
                            elegirNuevoDestino()
                        else
                            local distancia = (raiz.Position - destinoActual).Magnitude
                            if distancia < 4 then
                                elegirNuevoDestino()
                            else
                                hum:MoveTo(destinoActual)
                            end
                        end
                    end
                    tiempoSalto = tiempoSalto + 0.
